export type DashboardStat = {
  key: string;
  icon: string;
  iconBg: string;
  title: string;
  value: string;
  unit: string;
};

export type QuickAction = {
  key: string;
  icon: string;
  iconBg: string;
  iconColor: string;
  title: string;
  subtitle: string;
};

export type RecentActivity = {
  id: string;
  icon: string;
  iconBg: string;
  iconColor: string;
  title: string;
  time: string;
};